/*
package com.example.demo_back;


import static org.junit.jupiter.api.Assertions.assertEquals;

import com.example.demo_back.service.WordCounterService;
import org.junit.jupiter.api.Test;

public class WordCounterServiceTest {

    private final WordCounterService wordCounterService = new WordCounterService();

    @Test
    public void testCountWords() {
        assertEquals(18, wordCounterService.countWords("A user interface is like a joke.\nIf you have to explain it, it's not that good."));
        assertEquals(41, wordCounterService.countWords("Test data is data which has been specifically identified for use in tests, typically of a computer program.\n" +
                "Due to privacy rules and regulations like GDPR, PCI and HIPAA it is not allowed to use privacy sensitive personal data for testing."));
        assertEquals(34, wordCounterService.countWords("TDD is a software development process relying on software requirements being converted to test cases before software is fully developed, and tracking all software development by repeatedly testing the software against all test cases."));
        assertEquals(13, wordCounterService.countWords("What's the difference between a good joke and a bad joke timing."));
    }
}

*/
